name: rag
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - docling               # PDF → structured JSON / Markdown 
      - pypdf                 # simple PDF cut / merge
      - llama-index-core      # core LlamaIndex
      - llama-index-readers-docling   # DoclingReader integration 
      - llama-index-embeddings-openai   #embeddings
      - llama-index-node-parser-docling  # DoclingNodeParser integration 
      - openai                # (or swap for your preferred embedding client)
